import { StyleSheet } from "react-native";

export default StyleSheet.create({
    logoContainer: {
        justifyContent: "center",
        alignItems: "center",
        marginBottom: 16,
    },
    logo: {
        resizeMode: "contain",
        height: 180,
    },
    title: {
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 30,
        color: '#E5BF3C'
    }
});